{
  "name": "v2rayez-universal-firefox-extension",
  "version": "2.0.0",
  "description": "V2RayEZ Universal Firefox Extension — anti-censorship proxy manager",
  "private": true,
  "scripts": {
    "build": "node ../scripts/build-extension.mjs firefox",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "eslint": "^9.3.0"
  }
}
