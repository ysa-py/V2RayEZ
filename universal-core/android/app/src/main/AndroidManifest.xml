<?xml version="1.0" encoding="utf-8"?>
<!--
  V2RayEZ Universal Core — corrected, standardized manifest.

  Package identity is owned by the Gradle build (namespace/applicationId =
  "com.v2rayez.core") so that the Java sources, JNI symbol prefixes
  (Java_com_v2rayez_core_NativeBridge_*) and the manifest stay in perfect
  sync. A redundant `package` attribute is deprecated by AGP 8+ and only
  produces a build warning, so it is intentionally omitted. The application
  label is "V2RayEZ" (module heading: "V2RayEZ Universal Core").

  Corrected / standardized attributes:
    * android:installLocation="auto"      -> installable from any storage location
    * android:extractNativeLibs="true"   -> .so extracted at install time, avoiding
                                             page-alignment install parse errors on
                                             MIUI / rootless installers
    * android:foregroundServiceType="specialUse"
                                           -> correct Android 14 (API 34) foreground
                                             service declaration for the VPN core
    * All activities / services / features preserved 100% (zero deletion).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Preserved 100%: VpnService. It is the single real service; it is a
          foreground service by nature (BIND_VPN_SERVICE) and the
          FOREGROUND_SERVICE_SPECIAL_USE + dataSync declarations keep the tunnel
          alive on Android 14+ (API 34) without a MissingForegroundServiceType
          crash. No other feature/service is removed.
        -->
        <service
            android:name=".VpnService"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="VPN tunnel through the V2RayEZ Universal Core" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
