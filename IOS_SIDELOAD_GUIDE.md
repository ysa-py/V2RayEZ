# راهنمای نصب کلاینت iOS بدون کامپیوتر شخصی

> **نسخه: 2026-09-05** — این سند نتیجه‌ی بررسیِ مستند است، نه حدس. هر ادعا با منبع مشخص شده.

## ۱. نتیجه‌ی یک‌خطی (اول بخوانید)

شرط‌های شما پنج تا هستند: **بدون کامپیوتر شخصی**، **رایگان**، **پشتیبانی از iOS 12**، **بدون جیلبریک/روت**، **دائمی (بدون تمدید ۷ روزه)**.

**این پنج شرط با هم روی هیچ نسخه‌ای از iOS قابل جمع نیستند.** دو تا از آن‌ها باید کنار بروند:

| شرط | وضعیت | دلیلِ فنیِ مستند |
|---|---|---|
| بدون کامپیوتر شخصی | ✅ **ممکن** | بعد از یک‌بار Pairing حدوداً ۱۰ دقیقه‌ای (با هر کامپیوتری، حتی قرضی)، همه‌چیز روی خود گوشی انجام می‌شود [1](https://sidestore.io/) |
| رایگان | ✅ **ممکن** | اپل اکانت رایگان + ابزارهای متن‌باز؛ فقط سقف ۳ اپ و گواهی ۷ روزه دارد [3](https://ios18apps.com/altstore-vs-sideloadly-vs-sidestore/) |
| بدون جیلبریک/روت | ✅ **ممکن** | TrollStore و SideStore روی iOS کاملاً استوک کار می‌کنند [2](https://sourceforge.net/projects/trollstore.mirror/) |
| **دائمی، بدون تمدید ۷ روز** | ⚠️ **فقط روی iOS 14.0 تا 17.0** | فقط با TrollStore (باگ CoreTrust). اپل این باگ را در **17.0.1** بست [4](https://blog.sideloadhub.com/how-to-sideload-apps-iphone/) |
| **iOS 12** | ❌ **غیرممکن** | دو دلیل مستقل در ادامه |

پس **هوشمندانه‌ترین تصمیم** این است: شرط iOS 12 را کنار بگذارید (یا روی یک دستگاه دوم نگه دارید) و روی **iOS 15.0 تا 17.0** بروید، جایی که نصب شما **واقعاً و for-ever دائمی** است.

---

## ۲. چرا iOS 12 غیرممکن است (دو دلیل مستقل)

### دلیل ۱ — خودِ اپ روی iOS 12 اجرا نمی‌شود، حتی اگر امضا بشود

* `IPHONEOS_DEPLOYMENT_TARGET` پروژه روی **15.0** است (`MICAFP/ios/project.yml`).
* رابط کاربری پروژه با **SwiftUI** نوشته شده که از **iOS 13** به بعد وجود دارد، و کد از `async/await` استفاده می‌کند [5](https://cocoacasts.com/cocoa-fundamentals-everything-you-need-to-know-about-deployment-targets).
* Xcode 27 (ابزاری که روی Runner ساخته می‌شود) اساساً **Deployment Target کمتر از 15.0 را رد می‌کند**؛ محدوده‌ی مجاز آن 15.0 تا 27.0 است [6](https://bleepingswift.com/blog/deployment-target-supported-range-xcode-27).

یعنی حتی اگر جادو هم می‌شد و امضا حل می‌شد، باینری روی iOS 12 باز نمی‌شد. **پایین آوردن Deployment Target راه‌حل نیست** — باعث حذف بخش بزرگی از قابلیت‌ها می‌شود و باز هم با Xcode فعلی ساخته نمی‌شود.

### دلیل ۲ — هیچ مسیرِ «دائمی + رایگان + بدون کامپیوتر + بدون جیلبریک» روی iOS 12 وجود ندارد

* **TrollStore** (تنها ابزار واقعاً دائمیِ بدون جیلبریک) روی iOS 14.0 تا 17.0 کار می‌کند و **روی iOS 12 اصلاً پشتیبانی نمی‌شود** [4](https://blog.sideloadhub.com/how-to-sideload-apps-iphone/) [3](https://ios18apps.com/trollstore-vs-altstore/).
* **SideStore** (بهترین گزینه‌ی بدون کامپیوتر) حداقل **iOS 14** می‌خواهد [7](https://ios18apps.com/sidestore/).
* روی iOS 12 فقط **AltStore** (حداقل iOS 12.2) می‌ماند، که برای نصب اولیه و برای Refresh هر ۷ روز **به کامپیوتر نیاز دارد** [3](https://ios18apps.com/altstore-vs-sideloadly-vs-sidestore/).

> تنها راه دائمی روی iOS 12، جیلبریک (AppSync / Ext3nder) است — که خودتان آن را ممنوع کرده‌اید.

---

## ۳. مسیر پیشنهادی (به ترتیب اولویت)

### 🥇 گزینه A — TrollStore: دائمیِ واقعی (بهترین، اگر ورژن‌تان مجاز است)

* **محدوده:** iOS **14.0 تا 17.0**. نکته‌ی حیاتی: **17.0 بله، 17.0.1 به بعد خیر** [4](https://blog.sideloadhub.com/how-to-sideload-apps-iphone/).
* **دائمی:** بله. هیچ انقضایی، هیچ Revoke ای، هیچ امضای مجددی. باگ CoreTrust امضا را for-ever معتبر نگه می‌دارد [2](https://sourceforge.net/projects/trollstore.mirror/).
* **جیلبریک:** نه، لازم نیست.
* **کامپیوتر:** بعد از اینکه TrollStore روی دستگاه نشست، **هرگز** [3](https://ios18apps.com/trollstore-vs-altstore/).
* **هزینه:** صفر.

**بهترین نقطه برای شما:** چون اپ حداقل iOS 15 می‌خواهد، بازه‌ی طلایی **iOS 15.x تا 17.0** است. عملاً: اگر روی **iOS 16.6.1** یا **17.0** هستید، **همین امروز نصب کنید و دیگر به هیچ‌چیز نیاز ندارید**. اگر بالاتر رفته‌اید، متأسفانه دیگر نمی‌توانید برگردید (اپل فقط آخرین ورژن را امضا می‌کند).

گام‌ها:
1. فایل `Vor-*-ios-unsigned.ipa` را از صفحه‌ی Release دانلود کنید (در `Files` یا با Share → TrollStore).
2. TrollStore را باز کنید → تب **Apps** → آیکون `+` → فایل IPA را انتخاب کنید.
3. تمام. اپ برای همیشه نصب است.

### 🥈 گزینه B — SideStore: «دائمی در عمل» (اگر بالای 17.0.1 هستید)

* **محدوده:** iOS **14 و بالاتر**، شامل iOS 18/26 [7](https://ios18apps.com/sidestore/).
* **کامپیوتر:** فقط **یک‌بار** در ابتدا برای ساخت Pairing File (حدود ۱۰ دقیقه با هر کامپیوتری — کامپیوترِ قرضی، کتابخانه، کافی‌نت). بعد از آن فقط وای‌فای [1](https://sidestore.io/).
* **تمدید ۷ روزه:** گواهی فنی هنوز ۷ روزه است، اما SideStore **خودکار در پس‌زمینه Refresh می‌کند** — شما هیچ کاری نمی‌کنید و اپ هیچ‌وقت از کار نمی‌افتد. این نزدیک‌ترین چیز به «دائمی» روی این ورژن‌هاست [1](https://sidestore.io/) [8](https://techybuff.com/refresh-sidestore-sideloaded-automode/).
* **جیلبریک:** نه. **هزینه:** صفر.

گام‌ها:
1. یک‌بار AltServer را روی یک کامپیوتر (ویندوز/مک/لینوکس) نصب و SideStore را روی گوشی نصب کنید.
2. Pairing File را بسازید و وارد SideStore کنید (ترجیحاً با JitterbugPair).
3. `Vor-*-ios-unsigned.ipa` را در تب **My Apps** با `+` اضافه کنید.
4. از این لحظه به بعد: نصب و تمدیدِ خودکار، فقط روی خود گوشی.

### 🥉 گزینه C — AltStore (فقط اگر واقعاً مجبور به ماندن روی iOS 12 هستید)

* کار می‌کند (iOS 12.2+) و **بدون جیلبریک** است، اما **هر ۷ روز یک کامپیوتر روی همان وای‌فای** می‌خواهد [3](https://ios18apps.com/altstore-vs-sideloadly-vs-sidestore/).
* و همان‌طور که گفته شد، خود اپ به هر حال روی iOS 12 اجرا نمی‌شود (حداقل iOS 15).

---

## ۴. محدودیت‌هایی که باید بشناسید (با اپل‌اکانت رایگان)

| محدودیت | مقدار | راه دور زدنِ مشروع |
|---|---|---|
| اعتبار گواهی | ۷ روز | TrollStore (دائمی) یا Refresh خودکار SideStore |
| تعداد اپ همزمان | ۳ اپ | **LiveContainer** — چند اپ را داخل یک اپ اجرا می‌کند [9](https://bluepie.in/2026/01/jan-2026/) |
| Revoke توسط اپل | ممکن است | فقط TrollStore مصون است |

---

## ۵. چه چیزی در این مخزن تغییر کرده تا این مسیرها واقعاً کار کنند

قبلاً وقتی Runner امضای دیجیتال نداشت، فقط یک `xcarchive` منتشر می‌شد (که مستقیماً قابل نصب نیست). حالا:

* `scripts/ios-packaging.sh::ios_package_unsigned_ipa()` از **همان `.app` کامپایل‌شده‌ی واقعی** داخل `Vor.xcarchive` یک **IPA امضا‌نشده‌ی واقعی** می‌سازد.
* اجرایی‌بودن فایل (Mach-O) **دو بار** بررسی می‌شود: قبل از zip شدن و بعد از آن (روی خود فایل خروجی).
* `scripts/build-ios-ipa.sh` در حالت بدون امضا سه فایل منتشر می‌کند:
  * `Vor-<ver>-ios-unsigned.ipa` — همین را به TrollStore / SideStore / AltStore / Sideloadly بدهید؛
  * `Vor-<ver>-ios-unsigned.xcarchive.zip` — آرشیو کامل Xcode (شامل dSYM) برای توسعه‌دهندگان؛
  * `Vor-<ver>-ios-SIGNING-STATUS.txt` — توضیحِ صادقانه‌ی وضعیت امضا.
* این قرارداد در `tools/auto_fix_regression_gate.mjs` قفل شده است، یعنی هیچ‌وقت دوباره به «فایل ساختگی» برنمی‌گردیم.

> توجه صادقانه: IPA امضا‌نشده **به‌تنهایی روی iOS باز نمی‌شود**. iOS قبل از اجرا حتماً یک امضا می‌خواهد — ابزاری که در بالا معرفی شد دقیقاً همان امضا را با اپل‌اکانت **خودتان** انجام می‌دهد. این یک محدودیتِ اپل است، نه نقص فایل.

---

## ۶. توصیه‌ی نهایی من

1. اگر دستگاهتان روی **iOS 16.6.1 / 17.0** است (یا می‌تواند باشد): **TrollStore**. این دقیقاً همان «دائمی و بدون کامپیوتر و بدون جیلبریک و رایگان» است که خواستید.
2. اگر مجبورید بالای 17.0.1 بمانید: **SideStore**. نیاز به کامپیوتر فقط یک‌بار و حدود ۱۰ دقیقه، بعد از آن تمدید کاملاً خودکار.
3. iOS 12 را از این پروژه حذف کنید: نه امضا برایش دائمی می‌شود و نه خود اپ رویش اجرا می‌شود. تلاش برای پایین آوردن Deployment Target فقط باعث **حذف قابلیت** می‌شود — که خط قرمز ماست.
